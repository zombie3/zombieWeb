import React from 'react';
import Head from 'next/head'
import Nav from '../components/nav'

const Home = () => (
    <div>
    <Head>
      <title>About</title>
    </Head>

    <Nav />
  </div>
);

export default Home;